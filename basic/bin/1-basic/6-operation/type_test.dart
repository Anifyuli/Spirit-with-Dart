void main() {
  String cat = 'Joe';
  double age = 1.6;

  // Type test dipakai untuk mengecek suatu tipedata dari variabel apakah sesuai dengan yang ditanyakan dengan format
  // 
  print(cat is String);
  print(age is !double);
}
