void main() {
  String hello = "Hello";
   print(hello.codeUnits);   // Untuk menunjukkan daftar karakter UTF-16 yang dimiliki string
   print(hello.isEmpty);     // Untuk mengecek apakah string tersebut kosong
   print(hello.isNotEmpty);  // Untuk mengecek apakah string tersebut tidak kosong
   print("The length of the string is: ${hello.length}");   // Untuk mengetahui panjang string
}