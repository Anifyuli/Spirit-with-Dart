void main() {
  // assert dipakai untuk mengecek galat pada program
  var age = 20;
  assert(age != 20); // Unhandled exception: Assertion failed

  /*
  Cara untuk menjalankan kode Dart dalam mode assert
  $ dart --enable-asserts file_name.dart
  */
}
